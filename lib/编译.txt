1.执行
    git fetch --all
    git reset --hard origin/master
  强制更新文件
2.执行
  cd lib
  执行
  g++ main.cpp CameraOpt.cpp HttpRequest.cpp -fPIC -shared -o libHvDeviceJava.so  -L. -lHvDevice
3.执行
  cd ..
  执行
  ./run.sh

